apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "detection.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "detection.name" . }}
    helm.sh/chart: {{ include "detection.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "detection.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
