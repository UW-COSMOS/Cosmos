apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proposals.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "proposals.name" . }}
    helm.sh/chart: {{ include "proposals.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "proposals.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
